
{% macro colonne(colonne) %}
    
                        <div class="card " style="border: 2px dashed #25ec09;">

                         <div class="card-header  flex-wrap border-1 pt-0 pb-0">
                            <div class="card-title">
                                <h3 class="card-label">INFOS BAC<span class="d-block text-muted pt-0 font-size-sm"></h3>
                            </div>
                            
                        </div>
                            <div class="card-body">
                               <div class="row">
                                    <div class="col-md-2 ">
                                        {{ form_row(colonne.matricule) }}
                                    </div>
                                    <div class="col-md-2 ">
                                        {{ form_row(colonne.numero) }}
                                    </div>
                                    <div class="col-md-2 ">
                                        {{ form_row(colonne.serie) }}
                                    </div>
                                    <div class="col-md-2 ">
                                        {{ form_row(colonne.annee) }}
                                    </div>
                                    <div class="col-md-3 ">
                                  {#   {% if colonne.vars.value and colonne.vars.value.bac is not null %}
                                            {{ dump(colonne.vars.value.bac.id)}}
                                    {% endif %} #}
                                        {{ form_row(colonne.bac) }}
                                    </div>
                                    {% if colonne.vars.value and colonne.vars.value.bac is not null %}
                                            <div class="form-group col-md-1 ">
                                                {# <a class="btn btn-dark btn-sm"  style="margin-top: 20px;" target="_blank" title="Télécharger le document" href="{{ path('fichier_index', {id:  colonne.vars.value.bac.id}) }}" download> <i class="fe fe-upload"></i>
                                                    <i class="fa fa-download"></i></a> #}


                                                    <a class="btn btn-dark btn-sm" style="margin-top: 18px;" target="_blank" href="{{ path('fichier_index', {id:  colonne.vars.value.bac.id}) }}" download> <i class="bi bi-upload"></i>
                </a>
                                            </div>
                                        
                                        {% endif %}
                                    
                               </div>
                            </div>
                        </div><br>

{% endmacro  %}

{% macro prototype_ligne_encart(form) %}
    {# <div class="list-colonne_universitaire"  data-prototype="{{ _self.colonne(form.cursusUniversitaires.vars.prototype)|e }}"></div>
    <button type="button" data-protoname="__name__" class="btn btn-default add_line_universitaire btn-xs" id="add_ligne_universitaire"  data-protoclass=".list-colonne_universitaire" data-container=".column-container_universitaire">
        <i class="fa fa-plus"></i> Ajouter une ligne
    </button> #}
    <div class="column-container_encart proto-container_encart" data-childclass="">
        {% for item in form.encartBacs %}
            {{ _self.colonne(item|e) }}
        {% endfor %}
    </div>
{% endmacro %}